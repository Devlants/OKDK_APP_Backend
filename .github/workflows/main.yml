name: Deploy to EC2

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run commands on EC2
        run: |
          ssh -i home/ubuntu/.ssh/id_rsa ubuntu@ip-172-31-2-138 "echo hi"
